[project]
name = "xas-managed-agents"
version = "0.1.0"
description = "Xioma Automotive Managed Agents: billing dashboard + allocation repair (OR-Tools min-cost-flow)"
requires-python = ">=3.10"
dependencies = [
    "anthropic",
    "python-dotenv",
    "ortools",
]

[tool.uv]
# Scripts-only project — nothing to build/install for the project itself.
package = false
