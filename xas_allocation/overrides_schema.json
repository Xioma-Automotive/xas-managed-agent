{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "XAS allocation steering override",
  "description": "The typed object a planner's natural language compiles to (§6). The prompt moves weights and pins; it NEVER edits solver code. Same inputs + same override => byte-identical plan. A ledger entry wraps one of these. All dates are ISO 'YYYY-MM-DD'.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "pins": {
      "description": "Instruction-driven pins. 'defer' unpins an order and forbids arrivals before not_before.",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "action"],
        "properties": {
          "order": { "type": "string", "description": "Real order_id / SO line id (resolved from NL by the agent), e.g. 'SO-4000'." },
          "action": { "type": "string", "enum": ["defer"] },
          "not_before": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$", "description": "Earliest allowed delivery date, e.g. 2026-09-14." }
        }
      }
    },
    "boosts": {
      "description": "Multiplicative weight bumps on W(o) for a customer's orders.",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["customer", "weight_mult"],
        "properties": {
          "customer": { "type": "string", "description": "customer_id (resolved from a dealer name, e.g. 'Colmobil' -> CUST-001)." },
          "weight_mult": { "type": "number", "exclusiveMinimum": 0 }
        }
      }
    },
    "forbid": {
      "description": "Hard 'do not move' — pins an order to its incumbent vehicle/date.",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "action"],
        "properties": {
          "order": { "type": "string" },
          "action": { "type": "string", "enum": ["no_move"] }
        }
      }
    },
    "lambda": {
      "description": "Changed-date step penalty for this solve. If null/absent, the λ sweep default applies.",
      "type": ["integer", "null"],
      "minimum": 0
    },
    "scope": {
      "description": "General filter that DEFINES the working set: when present, only rows matching ALL given dimensions are re-allocatable and everything else stays pinned. This is how the agent works a slice ('allocate all Colmobil orders for August') and how a fix stays local. Omit/null to fall back to the disrupted set.",
      "type": ["object", "null"],
      "additionalProperties": false,
      "properties": {
        "customers": {
          "description": "customer_ids or dealer names to include (e.g. ['CUST-001'] or ['Colmobil']).",
          "type": "array",
          "items": { "type": "string" }
        },
        "models": {
          "description": "sales_models to include.",
          "type": "array",
          "items": { "type": "string" }
        },
        "po": {
          "description": "purchase orders to include, e.g. ['PO-150'] — matches rows currently allocated to that PO.",
          "type": "array",
          "items": { "type": "string" }
        },
        "from_date": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$", "description": "Earliest promised_date in scope, inclusive." },
        "to_date": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$", "description": "Latest promised_date in scope, inclusive." }
      }
    },
    "ttl": {
      "description": "Optional expiry as a date; the entry is skipped on replay once the pull date passes it, so a one-off nudge doesn't linger.",
      "type": ["string", "null"],
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    }
  }
}
