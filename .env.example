ANTHROPIC_API_KEY=

# --- Billing Dashboard Agent ---
BILLING_DATA_TOKEN=                    # optional at setup — add later to enable live billing access
BILLING_HOST=api.xiomautomotive.com   # placeholder — real billing API host
AGENT_ID=
ENV_ID=
VAULT_ID=

# --- XAS Allocation Agent (prototype: synthetic data, no credential needed) ---
XAS_HOST=                              # empty until the real XAS API exists (DECIDE-7)
XAS_DATA_TOKEN=                        # optional — attach once XAS_HOST is set
ALLOC_AGENT_ID=
ALLOC_ENV_ID=
ALLOC_VAULT_ID=
